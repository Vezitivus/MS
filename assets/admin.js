const ADMIN_KEY='ms-series-admin-session-v2';let db,currentSeason,currentResultActivityId='',adminSession=readAdminSession();const $=id=>document.getElementById(id);function readAdminSession(){try{return JSON.parse(localStorage.getItem(ADMIN_KEY)||'null')}catch{return null}}function saveAdminSession(v){localStorage.setItem(ADMIN_KEY,JSON.stringify(v));adminSession=v}function clearAdminSession(){localStorage.removeItem(ADMIN_KEY);adminSession=null}function busy(on,text='Ielādējam sēriju…'){$('syncText').textContent=text;$('syncOverlay').classList.toggle('show',on)}function authError(err){$('authMessage').textContent=err?.message||String(err);$('authMessage').classList.remove('hidden')}function setAuthMode(create){$('seriesLoginForm').classList.toggle('hidden',create);$('createSeriesForm').classList.toggle('hidden',!create);$('adminAuthTitle').textContent=create?'Izveidot sēriju':'Atvērt sēriju';$('adminAuthSub').textContent=create?'Nosaukums, privātais kods un 5 ciparu admin PIN.':'Ievadi sērijas kodu un administratora PIN.';$('authMessage').classList.add('hidden')}$('showCreateSeries').onclick=()=>setAuthMode(true);$('showSeriesLogin').onclick=()=>setAuthMode(false);$('seriesLoginForm').onsubmit=async e=>{e.preventDefault();busy(true,'Atveram sēriju…');try{const s=await MS.api('loginSeriesAdmin',{code:$('loginSeriesCode').value.trim().toUpperCase(),pin:$('loginAdminPin').value.trim()});saveAdminSession({seasonId:s.id,adminToken:s.adminToken});await startAdmin()}catch(err){busy(false);authError(err)}};$('createSeriesForm').onsubmit=async e=>{e.preventDefault();busy(true,'Izveidojam sēriju…');try{const s=await MS.api('createSeriesDirect',{name:$('createSeriesName').value.trim(),code:$('createSeriesCode').value.trim().toUpperCase(),adminPin:$('createAdminPin').value.trim(),bestCount:Number($('createBestCount').value)});saveAdminSession({seasonId:s.id,adminToken:s.adminToken});await startAdmin()}catch(err){busy(false);authError(err)}};$('adminLogout').onclick=()=>{clearAdminSession();location.reload()};async function startAdmin(){if(!adminSession?.seasonId||!adminSession?.adminToken){busy(false);return}$('authView').style.display='none';$('adminApp').classList.add('show');await reloadAdmin();busy(false)}async function reloadAdmin(){db=await MS.api('seriesAdminBootstrap',{seasonId:adminSession.seasonId,adminToken:adminSession.adminToken});currentSeason=db.season;$('activeSeriesName').textContent=currentSeason.name;$('adminBrand').textContent=currentSeason.name;fillSeries();renderAll()}function adminApi(action,payload={}){return MS.api(action,{...payload,seasonId:currentSeason.id,adminToken:adminSession.adminToken})}UI.$$('.tab').forEach(b=>b.onclick=()=>{UI.$$('.tab').forEach(x=>x.classList.toggle('active',x===b));UI.$$('.tab-panel').forEach(x=>x.classList.toggle('hidden',x.id!==b.dataset.tab))});function fillSeries(){$('seriesTitle').value=currentSeason.name;$('seriesCode').value=currentSeason.code;$('bestCount').value=currentSeason.bestCount||12;$('seriesAdminPin').value=''}$('seriesForm').onsubmit=async e=>{e.preventDefault();busy(true,'Saglabājam sēriju…');try{const payload={id:currentSeason.id,name:$('seriesTitle').value.trim(),code:$('seriesCode').value.trim().toUpperCase(),bestCount:Number($('bestCount').value)};if($('seriesAdminPin').value)payload.adminPin=$('seriesAdminPin').value.trim();const updated=await adminApi('saveSeasonDirect',payload);if(updated.adminToken){adminSession.adminToken=updated.adminToken;saveAdminSession(adminSession)}await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}};$('activityForm').onsubmit=async e=>{e.preventDefault();busy(true,'Saglabājam aktivitāti…');try{const item={id:$('activityId').value||UI.uid(),seasonId:currentSeason.id,name:$('activityName').value.trim(),startAt:new Date($('startAt').value).toISOString(),registrationOpenAt:$('openAt').value?new Date($('openAt').value).toISOString():'',registrationCloseAt:$('closeAt').value?new Date($('closeAt').value).toISOString():'',description:$('description').value.trim()};await adminApi('saveActivityDirect',item);$('activityForm').reset();$('activityId').value='';await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}};function renderAll(){renderActivities();renderPlayers();renderStandings();if(currentResultActivityId)renderActivityResultRows()}function renderActivities(){const rows=(db.activities||[]).sort((a,b)=>new Date(a.startAt)-new Date(b.startAt));$('activityList').innerHTML=rows.length?rows.map(a=>`<div class="row"><div class="row-main"><div class="row-title">${UI.esc(a.name)}</div><div class="row-sub">${UI.fmtDate(a.startAt)}</div></div><button class="btn" onclick="openActivityResults('${a.id}')">Rezultāti</button><button class="btn" onclick="editActivity('${a.id}')">Labot</button><button class="btn danger" onclick="removeActivity('${a.id}')">Dzēst</button></div>`).join(''):'<div class="card empty">Aktivitāšu vēl nav.</div>'}window.editActivity=id=>{const a=db.activities.find(x=>String(x.id)===String(id));$('activityId').value=a.id;$('activityName').value=a.name;$('startAt').value=a.startAt.slice(0,16);$('openAt').value=a.registrationOpenAt?.slice(0,16)||'';$('closeAt').value=a.registrationCloseAt?.slice(0,16)||'';$('description').value=a.description||'';scrollTo({top:0,behavior:'smooth'})};window.removeActivity=async id=>{if(!confirm('Dzēst aktivitāti un tās rezultātus?'))return;await adminApi('deleteActivityDirect',{id});currentResultActivityId='';$('activityResultsCard').classList.add('hidden');await reloadAdmin()};function seasonMembers(){const ids=new Set((db.memberships||[]).filter(m=>String(m.status||'active')!=='removed').map(m=>String(m.playerId)));return(db.players||[]).filter(p=>ids.has(String(p.id)))}function renderPlayers(){const board=UI.leaderboard({...db,seasons:[currentSeason]},currentSeason.id);$('playerList').innerHTML=board.length?board.map(p=>`<div class="row"><div class="rank">${p.rank}</div><img class="avatar sm" src="${p.image||`https://ui-avatars.com/api/?name=${encodeURIComponent(p.name)}`}" alt=""><div class="row-main"><div class="row-title">${UI.esc(p.name)}</div><div class="row-sub">${p.events} aktivitātes</div></div><div class="points">${p.total}</div></div>`).join(''):'<div class="card empty">Dalībnieku vēl nav.</div>'}function renderStandings(){const best=Math.max(1,Number(currentSeason.bestCount)||12),rows=UI.leaderboard({...db,seasons:[currentSeason]},currentSeason.id);$('standingsTitle').textContent=`Best ${best}`;$('standingsList').innerHTML=rows.length?rows.map(p=>`<div class="row"><div class="rank">${p.rank}</div><img class="avatar sm" src="${p.image||`https://ui-avatars.com/api/?name=${encodeURIComponent(p.name)}`}" alt=""><div class="row-main"><div class="row-title">${UI.esc(p.name)}</div><div class="row-sub">Skaitīti ${p.countedEvents} no ${p.events} · labākais ${p.bestScore}</div></div><div class="points">${p.total}</div></div>`).join(''):'<div class="card empty">Kopvērtējumam vēl nav datu.</div>'}window.openActivityResults=id=>{currentResultActivityId=id;$('activityResultsCard').classList.remove('hidden');renderActivityResultRows();setTimeout(()=>$('activityResultsCard').scrollIntoView({behavior:'smooth',block:'start'}),50)};$('closeActivityResults').onclick=()=>{currentResultActivityId='';$('activityResultsCard').classList.add('hidden')};function renderActivityResultRows(){const aid=currentResultActivityId,a=db.activities.find(x=>String(x.id)===String(aid));if(!a)return;$('activityResultsTitle').textContent=a.name;const players=seasonMembers().sort((a,b)=>a.name.localeCompare(b.name,'lv'));$('activityResultList').innerHTML=players.map(p=>{const reg=(db.registrations||[]).find(x=>String(x.activityId)===String(aid)&&String(x.playerId)===String(p.id)&&x.status!=='removed'),r=(db.results||[]).find(x=>String(x.activityId)===String(aid)&&String(x.playerId)===String(p.id));return`<div class="row"><input class="participant-toggle" data-player="${p.id}" type="checkbox" ${reg?'checked':''} style="width:22px;height:22px"><div class="row-main"><div class="row-title">${UI.esc(p.name)}</div></div><input class="score-input" data-player="${p.id}" type="number" min="0" step="0.01" value="${r?.points??''}" placeholder="Punkti"></div>`}).join('')||'<div class="card empty">Sērijā vēl nav dalībnieku.</div>';$('saveActivityResults').classList.toggle('hidden',!players.length)}$('saveActivityResults').onclick=async()=>{busy(true,'Saglabājam rezultātus…');try{for(const toggle of UI.$$('.participant-toggle',$('activityResultList'))){const playerId=toggle.dataset.player;await adminApi('adminRegisterDirect',{activityId:currentResultActivityId,playerId,status:toggle.checked?'registered':'removed'});const input=UI.$(`.score-input[data-player="${playerId}"]`,$('activityResultList'));if(toggle.checked&&input?.value!=='')await adminApi('saveResultDirect',{activityId:currentResultActivityId,playerId,points:Number(input.value)})}await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}};startAdmin().catch(err=>{clearAdminSession();busy(false);authError(err)});