(()=>{'use strict';
const ADMIN_KEY='ms-series-admin-session-v4';
let db={},currentSeason=null,currentResultActivityId='',adminSession=readSession();
const el=id=>document.getElementById(id);const all=(s,r=document)=>Array.from(r.querySelectorAll(s));
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const fmtDate=v=>{if(!v)return'—';const d=new Date(v);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('lv-LV',{dateStyle:'medium',timeStyle:'short'}).format(d)};
const uid=()=>crypto.randomUUID?crypto.randomUUID():'id-'+Date.now()+'-'+Math.random().toString(36).slice(2);
function readSession(){try{return JSON.parse(localStorage.getItem(ADMIN_KEY)||'null')}catch{return null}}
function saveSession(v){localStorage.setItem(ADMIN_KEY,JSON.stringify(v));adminSession=v}
function clearSession(){localStorage.removeItem(ADMIN_KEY);adminSession=null}
function busy(on,text='Ielādējam sēriju…'){el('syncText').textContent=text;el('syncOverlay').classList.toggle('show',on)}
function showError(err){el('authMessage').textContent=err?.message||String(err);el('authMessage').classList.remove('hidden')}
function setMode(create){el('seriesLoginForm').classList.toggle('hidden',create);el('createSeriesForm').classList.toggle('hidden',!create);el('adminAuthTitle').textContent=create?'Izveidot sēriju':'Atvērt sēriju';el('adminAuthSub').textContent=create?'Nosaukums, privātais kods un 5 ciparu admin PIN.':'Ievadi sērijas kodu un administratora PIN.';el('authMessage').classList.add('hidden')}
window.showCreateSeriesForm=()=>setMode(true);window.showSeriesLoginForm=()=>setMode(false);
el('seriesLoginForm').addEventListener('submit',async e=>{e.preventDefault();busy(true,'Meklējam sēriju…');el('authMessage').classList.add('hidden');try{const s=await MS.api('loginSeries',{code:el('loginSeriesCode').value.trim().toUpperCase(),pin:el('loginAdminPin').value.trim()});if(!s?.id||!s?.adminToken)throw new Error('Sērija netika atrasta.');saveSession({seasonId:s.id,adminToken:s.adminToken});await openAdmin()}catch(err){clearSession();showError(err)}finally{busy(false)}});
el('createSeriesForm').addEventListener('submit',async e=>{e.preventDefault();busy(true,'Saglabājam jauno sēriju…');el('authMessage').classList.add('hidden');try{const s=await MS.api('createSeries',{name:el('createSeriesName').value.trim(),code:el('createSeriesCode').value.trim().toUpperCase(),adminPin:el('createAdminPin').value.trim(),bestCount:Number(el('createBestCount').value)||12});if(!s?.id||!s?.adminToken)throw new Error('Google neatgrieza izveidotās sērijas datus.');saveSession({seasonId:s.id,adminToken:s.adminToken});await openAdmin()}catch(err){clearSession();showError(err)}finally{busy(false)}});
el('adminLogout').onclick=()=>{clearSession();location.reload()};
async function openAdmin(){if(!adminSession?.seasonId||!adminSession?.adminToken)return;busy(true,'Ielādējam sērijas datus…');db=await MS.api('seriesBootstrap',{seasonId:adminSession.seasonId,adminToken:adminSession.adminToken});if(!db?.season)throw new Error('Sērijas dati netika ielādēti.');currentSeason=db.season;el('authView').style.display='none';el('adminApp').classList.add('show');el('activeSeriesName').textContent=currentSeason.name;el('adminBrand').textContent=currentSeason.name;fillSeries();renderAll()}
function adminApi(action,payload={}){return MS.api(action,{...payload,seasonId:currentSeason.id,adminToken:adminSession.adminToken})}
all('.tab').forEach(b=>b.onclick=()=>{all('.tab').forEach(x=>x.classList.toggle('active',x===b));all('.tab-panel').forEach(x=>x.classList.toggle('hidden',x.id!==b.dataset.tab))});
function fillSeries(){el('seriesTitle').value=currentSeason.name||'';el('seriesCode').value=currentSeason.code||'';el('bestCount').value=currentSeason.bestCount||12;el('seriesAdminPin').value=''}
el('seriesForm').addEventListener('submit',async e=>{e.preventDefault();busy(true,'Saglabājam sēriju…');try{const p={id:currentSeason.id,name:el('seriesTitle').value.trim(),code:el('seriesCode').value.trim().toUpperCase(),bestCount:Number(el('bestCount').value)||12};if(el('seriesAdminPin').value.trim())p.adminPin=el('seriesAdminPin').value.trim();const u=await adminApi('saveSeries',p);if(u?.adminToken)saveSession({seasonId:currentSeason.id,adminToken:u.adminToken});await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}});
el('activityForm').addEventListener('submit',async e=>{e.preventDefault();busy(true,'Saglabājam aktivitāti…');try{await adminApi('saveActivity',{id:el('activityId').value||uid(),name:el('activityName').value.trim(),startAt:new Date(el('startAt').value).toISOString(),registrationOpenAt:el('openAt').value?new Date(el('openAt').value).toISOString():'',registrationCloseAt:el('closeAt').value?new Date(el('closeAt').value).toISOString():'',description:el('description').value.trim()});el('activityForm').reset();el('activityId').value='';await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}});
async function reloadAdmin(){db=await MS.api('seriesBootstrap',{seasonId:adminSession.seasonId,adminToken:adminSession.adminToken});currentSeason=db.season;el('activeSeriesName').textContent=currentSeason.name;el('adminBrand').textContent=currentSeason.name;fillSeries();renderAll()}
function members(){const ids=new Set((db.memberships||[]).filter(m=>String(m.status||'active')!=='removed').map(m=>String(m.playerId)));return(db.players||[]).filter(p=>ids.has(String(p.id)))}
function board(){const best=Math.max(1,Number(currentSeason.bestCount)||12);return members().map(p=>{const scores=(db.results||[]).filter(r=>String(r.playerId)===String(p.id)).map(r=>Number(r.points)||0).sort((a,b)=>b-a);const counted=scores.slice(0,best);return{...p,total:counted.reduce((a,b)=>a+b,0),events:scores.length,countedEvents:counted.length,bestScore:scores[0]||0}}).sort((a,b)=>b.total-a.total||b.bestScore-a.bestScore||String(a.name).localeCompare(String(b.name),'lv')).map((p,i,a)=>({...p,rank:i&&p.total===a[i-1].total&&p.bestScore===a[i-1].bestScore?a[i-1].rank:i+1}))}
function renderAll(){renderActivities();renderPlayers();renderStandings();if(currentResultActivityId)renderResultRows()}
function renderActivities(){const rows=(db.activities||[]).sort((a,b)=>new Date(a.startAt)-new Date(b.startAt));el('activityList').innerHTML=rows.length?rows.map(a=>`<div class="row"><div class="row-main"><div class="row-title">${esc(a.name)}</div><div class="row-sub">${fmtDate(a.startAt)}</div></div><button class="btn" data-results="${a.id}">Rezultāti</button><button class="btn" data-edit="${a.id}">Labot</button><button class="btn danger" data-delete="${a.id}">Dzēst</button></div>`).join(''):'<div class="card empty">Aktivitāšu vēl nav.</div>';all('[data-results]',el('activityList')).forEach(b=>b.onclick=()=>openResults(b.dataset.results));all('[data-edit]',el('activityList')).forEach(b=>b.onclick=()=>editActivity(b.dataset.edit));all('[data-delete]',el('activityList')).forEach(b=>b.onclick=()=>removeActivity(b.dataset.delete))}
function editActivity(id){const a=(db.activities||[]).find(x=>String(x.id)===String(id));if(!a)return;el('activityId').value=a.id;el('activityName').value=a.name;el('startAt').value=String(a.startAt).slice(0,16);el('openAt').value=String(a.registrationOpenAt||'').slice(0,16);el('closeAt').value=String(a.registrationCloseAt||'').slice(0,16);el('description').value=a.description||'';scrollTo({top:0,behavior:'smooth'})}
async function removeActivity(id){if(!confirm('Dzēst aktivitāti un tās rezultātus?'))return;busy(true,'Dzēšam aktivitāti…');try{await adminApi('deleteActivity',{id});currentResultActivityId='';el('activityResultsCard').classList.add('hidden');await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}}
function renderPlayers(){const rows=board();el('playerList').innerHTML=rows.length?rows.map(p=>`<div class="row"><div class="rank">${p.rank}</div><img class="avatar sm" src="${p.image||`https://ui-avatars.com/api/?name=${encodeURIComponent(p.name)}`}" alt=""><div class="row-main"><div class="row-title">${esc(p.name)}</div><div class="row-sub">${p.events} aktivitātes</div></div><div class="points">${p.total}</div></div>`).join(''):'<div class="card empty">Dalībnieku vēl nav.</div>'}
function renderStandings(){const rows=board();el('standingsTitle').textContent=`Best ${Math.max(1,Number(currentSeason.bestCount)||12)}`;el('standingsList').innerHTML=rows.length?rows.map(p=>`<div class="row"><div class="rank">${p.rank}</div><img class="avatar sm" src="${p.image||`https://ui-avatars.com/api/?name=${encodeURIComponent(p.name)}`}" alt=""><div class="row-main"><div class="row-title">${esc(p.name)}</div><div class="row-sub">Skaitīti ${p.countedEvents} no ${p.events} · labākais ${p.bestScore}</div></div><div class="points">${p.total}</div></div>`).join(''):'<div class="card empty">Kopvērtējumam vēl nav datu.</div>'}
function openResults(id){currentResultActivityId=id;el('activityResultsCard').classList.remove('hidden');renderResultRows();setTimeout(()=>el('activityResultsCard').scrollIntoView({behavior:'smooth',block:'start'}),50)}
el('closeActivityResults').onclick=()=>{currentResultActivityId='';el('activityResultsCard').classList.add('hidden')};
function renderResultRows(){const a=(db.activities||[]).find(x=>String(x.id)===String(currentResultActivityId));if(!a)return;el('activityResultsTitle').textContent=a.name;const players=members().sort((a,b)=>a.name.localeCompare(b.name,'lv'));el('activityResultList').innerHTML=players.map(p=>{const reg=(db.registrations||[]).find(r=>String(r.activityId)===String(currentResultActivityId)&&String(r.playerId)===String(p.id)&&r.status!=='removed');const result=(db.results||[]).find(r=>String(r.activityId)===String(currentResultActivityId)&&String(r.playerId)===String(p.id));return`<div class="row"><input class="participant-toggle" data-player="${p.id}" type="checkbox" ${reg?'checked':''} style="width:22px;height:22px"><div class="row-main"><div class="row-title">${esc(p.name)}</div></div><input class="score-input" data-player="${p.id}" type="number" min="0" step="0.01" value="${result?.points??''}" placeholder="Punkti"></div>`}).join('')||'<div class="card empty">Sērijā vēl nav dalībnieku.</div>';el('saveActivityResults').classList.toggle('hidden',!players.length)}
el('saveActivityResults').onclick=async()=>{busy(true,'Saglabājam rezultātus…');try{for(const toggle of all('.participant-toggle',el('activityResultList'))){const playerId=toggle.dataset.player;await adminApi('adminRegister',{activityId:currentResultActivityId,playerId,status:toggle.checked?'registered':'removed'});const input=el('activityResultList').querySelector(`.score-input[data-player="${playerId}"]`);if(toggle.checked&&input?.value!=='')await adminApi('saveResult',{activityId:currentResultActivityId,playerId,points:Number(input.value)})}await reloadAdmin()}catch(err){alert(err.message)}finally{busy(false)}};
if(adminSession?.seasonId&&adminSession?.adminToken){openAdmin().catch(err=>{clearSession();showError(err)}).finally(()=>busy(false))}else busy(false);
})();